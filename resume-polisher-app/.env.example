# Environment Configuration Example
# Copy this file to .env and fill in your values

NODE_ENV=development

# Note: API key is entered in the app UI, not stored in .env
# This ensures the key is never committed to version control
